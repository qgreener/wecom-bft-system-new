# 99-统一校验报告

## 修复复核结果（2026-05-14）

本节为基于当前文档状态的复核结果。下方“阻塞问题 / 重要问题 / 一般问题”等原始清单保留修复前记录，用于追溯问题来源；实际状态以本节为准。

| 分类 | 当前状态 | 处理结果 |
|---|---|---|
| P0-01、P0-02 | 已解决 | 00/01 的实际文件名引用已对齐，00 清单已覆盖 13/14/15，并补充后端开发阅读 15 的可选入口；额外修正 03/06 中残留的 `10-部署与演示说明.md` 引用。 |
| P1-01 | 已解决 | PC 管理端保留“物流”作为页面展示语义，同时补充并强化“物流状态=履约状态”的术语映射，订单筛选和标签以“履约状态”为主。 |
| P1-02、P1-03、P1-07、P2-10 | 已解决 | 06 新增健康检查、Mock 场景控制、回调验签失败统一响应、三端受控文件下载接口；07 同步 Mock 场景控制联调口径。 |
| P1-04、P1-06、P2-03、P2-04 | 已解决 | 04/05/06 已补齐 `PaymentResult`、订单 `user_id`、学习权益 `course_snapshot`、采购明细 `received_quantity` 口径。 |
| P1-05、P2-06 | 已解决 | 09 已为 Mock 场景矩阵补充 `TC-MOCK-*` 用例编号，并新增学习权益状态流转验收项。 |
| P1-08、P2-07 | 已解决 | 10 的前序修订建议已改为落实记录；12/14 已回写健康检查、部署配置、Mock、文件下载等修订项。 |
| P2-01、P2-02、P2-08、P2-09 | 已解决或已判定合理 | 订单管理已包含“无需发货”和“未申请”筛选；小程序订单筛选已补充履约/开票状态；00 的 10 号文档描述已覆盖服务器部署；07 已补充财务软件直连预留适配层。 |
| Q-01 ~ Q-05 | 已确认 | 原型冻结、ID 发号策略、最终部署方式、Mock 回调路径策略和初始化文档口径均已回写到对应文档。 |

## 原始总体结论（修复前记录）

本次对「原型设计方案」（6 篇）与「新系统开发设计文档」（16 篇，含 13/14/15）共 22 份 Markdown 文档进行了 11 个维度的交叉校验。

**总体评价：文档体系基本完整，核心链路闭环可追溯，状态拆分口径一致，权限矩阵覆盖全面，测试验收与异常补偿场景充分。** 新系统设计严格遵循"不继承旧 wecom-bft-system"的边界约束，未发现引用旧代码、旧数据库、旧接口作为设计依据的情况。

**主要问题集中在：文档编号/文件名引用不一致（00 与 01 对下游文件名的引用与实际文件名存在多处偏差）、00 文档清单未覆盖 13/14/15、PC 管理端原型中的"物流状态"与设计文档的"履约状态"存在术语混用、以及部分接口缺少健康检查端点。** 这些问题不阻塞设计理解，但会在后续开发、联调和毕设答辩时造成查找困难。

原始报告共发现 **阻塞问题 P0：2 项**，**重要问题 P1：8 项**，**一般问题 P2：10 项**，**可直接修正项：6 项**，**需确认问题：5 项**；当前确认状态见上方“最终确认口径”。

---

## 原始阻塞问题 P0（修复前记录）

| 编号 | 严重级别 | 涉及文档 | 问题描述 | 影响 | 建议处理 |
|---|---|---|---|---|---|
| P0-01 | P0 | 00-文档说明.md、01-需求规格说明书.md | **00 文档清单中 10 号文件名为 `10-部署与演示说明.md`，但实际文件名为 `10-服务器部署与演示说明.md`。** 01 文档第 11 行"下游文档"列表中同样引用 `10-部署与演示说明.md`（缺少"服务器"）。此外 01 还引用 `07-外部系统联调.md`（缺少"文档"二字）和 `11-业务规则与算法设计.md`（缺少"关键"二字）。这些引用名称与实际文件名不一致。 | 读者按文档引用名称无法直接在文件系统中定位文件；CI/CD 或自动化脚本依赖文件名时将失败；毕设答辩时评委按文档清单查找会困惑。 | 统一 00 和 01 中引用的文件名为实际文件名，或在各文件中统一使用一种命名。建议以实际文件名为准修正 00 和 01。 |
| P0-02 | P0 | 00-文档说明.md | **00 文档清单只列到 14 号文档，但实际存在 15 号文档（`15-前端架构与设计系统落地规范.md`）。** 00 的阅读顺序表中也未包含 15 号文档。同时 13 号文档在文档清单中缺失（阅读顺序表后端开发行提到了 13 和 14，但清单表中未列出 13）。 | 15 号文档对于前端开发是关键参考，缺少入口指引会导致前端团队遗漏该文档；文档体系的完整性在评审时存疑。 | 在 00 第 4 节文档清单中补充 13、14、15 号文档的条目；在第 5 节阅读顺序中为前端开发补充 15，为后端开发补充 15（若需要）。 |

---

## 原始重要问题 P1（修复前记录）

| 编号 | 严重级别 | 涉及文档 | 问题描述 | 影响 | 建议处理 |
|---|---|---|---|---|---|
| P1-01 | P1 | PC管理端.md、02-业务流程与状态机说明.md、03-模块设计文档.md | **PC 管理端原型多处使用"物流状态"来指代发货/履约状态**（如第 77 行"物流状态=待发货"、第 392 行"物流状态筛选"、第 398 行"物流状态（待发货/已发货/已签收）"）。但新系统设计文档统一使用"履约状态"（fulfillment_status）命名，且系统设计约束.md 第 48 行也使用"履约状态"。术语不统一。 | 前端开发参照原型开发时可能用"物流状态"字段名，与后端 API 返回的 `fulfillment_status` 不一致；接口联调时产生字段名冲突。 | 在原型文档备注中说明"物流状态即设计文档中的履约状态"，或在 PC 管理端原型文档中统一改为"履约状态（物流）"。不需要改设计文档，原型文档是 UI 视角，标注映射关系即可。 |
| P1-02 | P1 | 06-API接口文档.md、10-服务器部署与演示说明.md | **10 号文档第 470 行指出需在 06 中补充健康检查接口**（如 `/api/health`），但 06 当前未定义任何健康检查端点。10 号文档明确将此项列为前序文档修订建议。 | 部署和运维缺少标准化的健康检查入口；Nginx 代理、Docker Compose 或 K8s 探活无法正常工作；云平台和负载均衡无法判断服务可用性。 | 在 06 中新增健康检查接口定义，至少包含 `/api/health`（基本存活）和 `/api/health/ready`（依赖就绪检查：数据库、Redis、文件存储）。同时更新 14 号文档的接口规范。 |
| P1-03 | P1 | 06-API接口文档.md、07-外部系统联调文档.md | **10 号文档第 473 行建议补充 Mock 场景控制接口到 06/07。** 当前 06 没有定义任何 Mock 场景的查询、切换、投递回调接口；07 只在联调说明中描述了 Mock 行为但未给出操作入口的接口契约。 | 测试和演示人员无法通过 API 或管理页面切换 Mock 场景（如"下次支付返回金额不一致"），只能改数据库或配置重启，极大影响测试效率和演示流畅度。 | 在 06 中至少新增一个 `GET /api/admin/mock/scenes`（查询可用场景）和 `POST /api/admin/mock/scenes/{scene}/trigger`（触发指定场景），并在 07 中补充对应的联调说明。 |
| P1-04 | P1 | 01-需求规格说明书.md、05-数据库设计文档.md | **05 数据库设计中 `pay_payment.payment_result` 使用了独立的支付结果枚举（`SUCCESS`、`FAILED`、`PROCESSING`、`CLOSED`），但 04 领域模型的支付记录字段列表中未定义 `payment_result` 的枚举字典。** 04 的枚举字典 5.1 节只定义了订单级 `PaymentStatus`，支付记录的 `PaymentResult` 枚举缺失。 | 接口和数据库使用支付结果字段时缺少统一的枚举定义来源；前后端对支付结果的取值可能不一致。 | 在 04 的 5.1 或 5.2 节补充 `PaymentResult` 枚举（`SUCCESS`、`FAILED`、`PROCESSING`、`CLOSED`），与 05 和 06 保持一致。 |
| P1-05 | P1 | 01-需求规格说明书.md、09-测试与验收用例.md | **09 测试用例覆盖了 01 的 15 条 P0 需求，但缺少对 P0-15（Mock 外部能力保证演示闭环）的独立用例编号映射。** 09 的 P0 验收通过清单中 P0-15 引用了"外部接口 Mock 场景矩阵、E2E-010"，但矩阵中的各个 Mock 场景没有独立的可追踪用例编号（格式为 TC-XXX-M-XXX）。 | P0-15 的验收证据采集缺少明确的可逐项勾选的用例列表；毕设答辩时评委可能质疑"演示闭环"的测试覆盖是否充分。 | 为 09 第 6 节 Mock 场景矩阵中的每个场景补充用例编号（如 TC-MOCK-PAY-001 等），或在该节开头说明矩阵中的每一项映射到哪些已有用例编号。 |
| P1-06 | P1 | 05-数据库设计文档.md、06-API接口文档.md | **05 的订单表 `trade_order` 中 `user_id` 字段未在 04 的订单领域对象中显式定义。** 04 订单对象（6.9 节）只列出了 `student_id`，但 05 订单表包含了 `user_id BIGINT`。06 的订单相关接口也未提及 `user_id`。 | 数据库需要 `user_id` 来支持按用户维度的订单查询（如学员合并后的历史订单追溯），但接口和领域模型缺失该字段定义会导致前后端口径不一致。 | 在 04 订单对象中补充 `user_id` 字段并在 06 的订单响应字段中体现（至少管理端接口返回）。 |
| P1-07 | P1 | 02-业务流程与状态机说明.md、06-API接口文档.md | **06 外部回调接口（第 8 节）未定义统一的回调验签失败响应格式。** 02 和 12 都要求验签失败拒绝业务处理，但 06 只给出了回调路径和幂等键，没有说明验签失败时应返回的 HTTP 状态码和响应体格式。不同平台的回调可能对验签失败有不同的重试策略。 | 外部平台回调验签失败时，后端可能返回不合适的响应码导致平台无限重试或错误告警；安全事件无法准确追踪。 | 在 06 第 8 节开头补充回调通用处理规则：验签失败返回 `401` + 固定错误码（如 `CALLBACK_SIGN_FAILED`），且不写入业务单据；验签通过但业务处理失败返回 `200`/`202` + 业务错误码以告知平台结果。 |
| P1-08 | P1 | 10-服务器部署与演示说明.md、14-工程结构与开发规范.md | **10 号文档第 471-473 行列出了 4 条对前序文档的修订建议，但这些建议尚未合并到对应文档中。** 特别是关于 `.env.example` 应包含哪些部署配置项、Docker Compose 文件名、systemd 服务文件名的约定尚未在 14 中落实。 | 部署文档和前序文档的口径不一致会持续扩大；新加入的开发人员不知道哪些是"待确认"项、哪些是"已确认"项。 | 在完成本报告修正后，将 10 的 4 条修订建议逐条落实到 06、07、14 中，并在 10 中标注"已落实"。 |

---

## 原始一般问题 P2（修复前记录）

| 编号 | 严重级别 | 涉及文档 | 问题描述 | 影响 | 建议处理 |
|---|---|---|---|---|---|
| P2-01 | P2 | PC管理端.md | **PC 管理端原型第 77 行"物流状态（待发货/已发货/已签收）"中未包含"无需发货"状态。** 对于纯虚拟课程订单，订单管理筛选区域应展示"无需发货"选项。 | 管理端订单筛选不完整，纯虚拟课程订单的履约状态无法被准确筛选。 | 在 PC 管理端原型订单管理的履约状态筛选中补充"无需发货"选项。 |
| P2-02 | P2 | 小程序端.md、PC管理端.md | **小程序端和 PC 管理端原型的开票状态筛选中均未包含"未申请"选项。** 小程序第 135 行退款状态筛选中列出了全部 7 种状态，但开票状态未作为筛选维度出现。PC 第 370 行开票管理的筛选只包含"已申请/待开具/已开具/已红冲"，缺少"未申请"。 | 订单管理的开票状态筛选不完整，但不影响核心流程（未申请通常作为默认筛选状态）。 | 在小程序端订单列表增加开票状态筛选；PC 开票管理筛选保持当前口径即可（因为"未申请"状态下没有发票单，不在开票管理中展示是合理的）。可在订单管理页的开票状态筛选中体现。 |
| P2-03 | P2 | 04-领域模型与数据字典.md、05-数据库设计文档.md | **04 学习权益对象（6.8 节）包含 `course_snapshot` 字段用于课程快照，但 05 的 `learning_entitlement` 表结构中未列出 `course_snapshot` 对应的数据库字段。** 05 只列出了权益表的核心字段，快照字段遗漏。 | 权益追溯时无法获取下单时的课程名称、规格名称等展示信息；订单详情单据链中的权益节点缺少可读信息。 | 在 05 的 `learning_entitlement` 表结构中补充 `course_snapshot JSON` 字段。 |
| P2-04 | P2 | 05-数据库设计文档.md、03-模块设计文档.md | **05 的 `purchase_order_item` 表包含 `received_quantity INT` 字段，但 04 的采购明细（包含在采购单的 `purchase_lines` JSON 中）未定义该字段。** 实际收货数量与采购数量可能不一致，该字段用于记录差异。 | 收货差异场景无法在领域模型中准确表达；接口返回的采购明细中收货数量缺失。 | 在 04 采购单的 `purchase_lines` 说明中补充 `received_quantity` 字段；或在 05 中明确 `purchase_receipt` 表的 `received_items` 如何关联到采购明细。 |
| P2-05 | P2 | 06-API接口文档.md | **06 小程序端接口缺少"继续支付"接口的订单详情中是否返回剩余支付时间的说明。** 第 102 行拉起支付接口返回了 `payment_expire_at`，但未说明前端如何计算倒计时。 | 前端可能自行计算倒计时导致与服务端不一致；超时判断出现偏差。 | 在 06 的订单列表/详情接口中明确返回 `payment_expire_at` 和当前服务端时间，由前端据此计算剩余时间而非依赖客户端时钟。 |
| P2-06 | P2 | 09-测试与验收用例.md | **09 的状态流转验收清单未包含学习权益状态流转（`ACTIVE` → `FROZEN` → `REVOKED`）的独立验收项。** 权益状态流转在 02 和 04 中有完整定义，但 09 只在退款相关用例中间接验证。 | 学习权益作为核心领域对象，其独立状态流转缺乏专项验收用例；冻结与撤销的边界条件（如冻结后恢复）未覆盖。 | 在 09 第 7 节"状态流转验收清单"中增加学习权益状态流转验收项。 |
| P2-07 | P2 | 12-异常处理与幂等补偿设计.md | **12 号文档第 226 行"前序文档修订建议"标注为"暂无"，但至少存在前文所述 P1-02（健康检查接口缺失）和 P1-08（10 号文档对前序文档的修订建议未落实）。** | 12 作为总结性异常设计文档，其修订建议为空，与实际情况不符；可能误导后续修订工作认为无需改动前序文档。 | 在 12 的修订建议中至少列出一条指向 P1-02 的建议条目。该条目不要求详细展开，只需指向具体问题即可。 |
| P2-08 | P2 | 00-文档说明.md、10-服务器部署与演示说明.md | **00 第 4 节文档清单中 10 号文档的目标描述为"支持本地运行、Mock 演示、初始化数据和答辩演示"，但实际 10 号文档的范围远大于此**（包含完整的服务器部署、Nginx 配置、HTTPS 要求、备份恢复、环境变量清单等内容）。 | 仅看 00 的简介，读者可能认为 10 只是本地/演示文档而忽略其服务器部署指导价值。 | 在 00 第 4 节更新 10 号文档的目标描述，改为"支撑服务器与本地部署、Mock/沙箱/真实接入切换、初始化数据、演示流程和运维排查"。 |
| P2-09 | P2 | 原型设计方案/README.md、01-需求规格说明书.md | **原型 README 第 6 行提到"财务软件直连作为扩展能力"，01 需求文档第 242 行也将"财务软件"列为扩展能力（P2-01）。但除这两处外，整个新系统设计文档中未再定义财务软件的接口适配层、数据导入导出格式、或与代账工作台的关系。** | P2 扩展能力缺少最小设计定义，未来若实现该需求将需要重新设计全链路；但目前作为 P2 扩展能力，缺少设计不阻塞 P0/P1。 | 在 07 外部系统联调文档中增加一段（约 5-8 行）说明财务软件直连的预留适配层位置和数据交互方向（如导出科目明细、导入凭证），标注为"扩展能力，当前不实现"。 |
| P2-10 | P2 | 03-模块设计文档.md、06-API接口文档.md | **03 第 8 节"统一接口分类"中提到了"文件导入导出接口"类别，但 06 中文件上传接口（`POST /api/admin/files`）只定义了上传，没有定义统一的文件下载鉴权接口。** 发票文件、红冲凭证、退款凭证、代账材料等下载散落在各业务接口中，缺少统一的受控文件下载端点。 | 前端实现文件下载时需要逐个业务接口处理鉴权逻辑；Nginx 直出文件的安全策略难以统一实施；10 号文档中要求的"受控下载"缺少 API 支撑。 | 在 06 中新增统一的文件下载接口 `GET /api/admin/files/{file_no}/download`（以及对应的 app/supplier-h5 端点），后端根据 `file_no` 查 `sys_file_asset` 并校验权限后返回文件流或临时签名 URL。 |

---

## 原始可直接修正项（修复前记录）

| 编号 | 涉及文档 | 修正内容 | 修正后状态 |
|---|---|---|---|
| FIX-01 | 00-文档说明.md | 第 48 行 `10-部署与演示说明.md` → `10-服务器部署与演示说明.md` | 与实际文件名一致 |
| FIX-02 | 01-需求规格说明书.md | 第 11 行下游文档列表中 `07-外部系统联调.md` → `07-外部系统联调文档.md`；`10-部署与演示说明.md` → `10-服务器部署与演示说明.md`；`11-业务规则与算法设计.md` → `11-关键业务规则与算法设计.md` | 与实际文件名一致 |
| FIX-03 | 00-文档说明.md | 第 59 行后端开发推荐阅读顺序中补充 `15`（前端架构对后端开发非必读，但可标注为"可选"） | 阅读顺序完整 |
| FIX-04 | 00-文档说明.md | 第 60 行前端开发推荐阅读顺序中补充 `15` | 阅读顺序完整 |
| FIX-05 | 00-文档说明.md | 第 48 行文档清单中补充：`\| 15 \| \`15-前端架构与设计系统落地规范.md\` \| 明确前端技术栈、组件体系、视觉语言、页面模板和跨端一致性规范 \|` | 文档清单完整 |
| FIX-06 | PC管理端.md | 第 77 行程式管理筛选区的履约状态筛选中补充"无需发货"选项，在第 251 行订单管理的履约状态筛选文字中增加"无需发货" | 筛选维度完整 |

---

## 最终确认口径

| 编号 | 最终口径 | 依据 |
|---|---|---|
| Q-01 | 原型设计方案冻结，新系统开发设计文档为唯一真相源。 | 原型 README 与 00 已写明原型仅作需求与页面参考。 |
| Q-02 | 统一使用雪花 ID，应用层发号，不依赖数据库自增。 | 05 的 ID 约束已改为统一雪花 ID。 |
| Q-03 | 最终服务器部署，默认采用 Docker Compose，一键编排后端、数据库、Redis、MinIO 和 Nginx；systemd 作为备选。 | 10、13、14 已统一该口径。 |
| Q-04 | Mock 与真实回调共用同一 `/api/callbacks/**` 路径，通过适配层、签名校验和环境模式区分。 | 06、07 已明确共用回调路径。 |
| Q-05 | 种子数据具体内容写入 10 号文档附录。 | 10 已补演示种子数据最小清单。 |

---

## 原始建议修订顺序（修复前记录）

1. **立即处理**（P0，约 30 分钟）：
   - FIX-01 ~ FIX-05：修正 00 和 01 中的文件名引用
   - FIX-06：修正 PC 管理端原型的履约状态筛选

2. **优先处理**（P1，约 2-4 小时）：
   - P1-02：在 06 中补充健康检查接口
   - P1-04：在 04 中补充 `PaymentResult` 枚举
   - P1-06：在 04 订单对象中补充 `user_id`
   - P1-03：在 06/07 中补充 Mock 场景控制接口
   - P1-07：在 06 中补充回调验签失败响应规范
   - P1-01：在 PC 管理端原型中标注"物流状态→履约状态"术语映射
   - P1-05：在 09 中补充 Mock 场景的用例编号
   - P1-08：将 10 的修订建议逐条落实到对应文档并更新状态

3. **后续处理**（P2，约 1-2 小时）：
   - P2-03：在 05 权益表中补充 `course_snapshot`
   - P2-04：在 04 采购明细中补充 `received_quantity`
   - P2-10：在 06 中补充统一文件下载接口

4. **用户确认后再处理**：
   - Q-01 ~ Q-05：已按最终确认口径执行

---

## 原始各维度校验详情（修复前记录）

### 一、文档结构与编号

**通过项：**
- 原型方案 6 篇文档齐全，新系统设计文档 00-15 共 16 篇齐全
- 各文档之间无循环引用，阅读顺序合理
- 文档均为 UTF-8 Markdown，读取正常

**问题项：** 见 P0-01、P0-02、FIX-01 ~ FIX-05

### 二、新系统边界

**通过项：**
- 00 号文档第 1/3/7 节明确声明不继承旧系统
- 01-15 每篇文档开头均包含"不继承历史失败产物"的边界声明
- 未发现任何"参考旧项目代码"或"沿用旧表结构"的表述
- 12 号文档第 1 节明确"不改变已定义的模块边界、领域模型、数据库表"

**问题项：** 无阻塞问题。边界约束执行一致。

### 三、术语一致性

**通过项：**
- 角色名称（超级管理员、教务、讲师、运营、仓管、客服、代账人员）在所有文档中保持一致
- 端侧名称（PC 管理端、小程序端、企微/外部协同端）在使用中一致（虽原型中端名略有差异但语义相同）
- 单据名称（支付记录、学习权益、发货单、退款单、发票单、对账记录、代账材料）在 02-12 中高度一致
- 模块名称在 03-08 中一致

**问题项：** 见 P1-01（物流状态 vs 履约状态在原型中的混用）

### 四、状态枚举一致性

**通过项：**
- 订单四类状态拆分在全部文档中坚持执行，未发现任何使用单一 `order_status` 的地方
- 支付状态枚举（待支付/已支付/已关闭）—— 一致
- 履约状态枚举（无需发货/待发货/已发货/已签收）—— 一致
- 退款状态枚举（无退款/审核中/已拒绝/退款处理中/待人工退款/退款失败/已退款）—— 一致
- 开票状态枚举（未申请/已申请/待开具/已开具/已红冲）—— 一致
- 采购状态枚举（审批中/审批拒绝/待确认/已确认/已发货/已拒绝/已完成/已取消）—— 一致
- 未发现旧状态残留（待付款、已取消作为订单总状态、已通过作为退款页面状态等）

**问题项：** 见 P1-04（`PaymentResult` 枚举在 04 中缺失定义）

### 五、核心链路闭环

**通过项：**
- 线索→学员→订单→支付记录→学习权益/发货单→退款单→发票单→对账记录→代账材料 链路完整
- 02/04/05/06/11 中每个环节都有对应的触发条件、状态流转、副作用和数据表
- 支付成功后置动作（支付记录、学习权益、待发货任务、可申请开票、通知、线索转化回写）在 01/02/11 中完整定义
- 退款成功副作用（冻结/撤销权益、停止提醒、已开票生成红冲待办、含实物判断退货/人工确认、对账影响）在 02/11/12 中完整定义
- 大额采购先审批再推送供货商的约束在所有文档中一致
- 已开票退款进入红冲流程的约束完整
- 对账差异可回到订单和支付/退款记录追溯

**问题项：** 无阻塞问题。核心链路闭环完整。

### 六、模块设计与领域模型一致性

**通过项：**
- 03 的 12 个模块与 04 的核心领域对象一一对应
- 04 的 21 个领域对象均有明确归属模块
- 05 的数据表与 04 的领域对象对应关系清晰
- 06 API 覆盖了 12 个模块的核心能力
- 08 权限矩阵覆盖了 03 的模块和菜单映射

**问题项：** 见 P1-06（`user_id` 在订单对象中缺失）、P2-03（`course_snapshot` 在权益表中缺失）、P2-04（`received_quantity` 在领域模型中缺失）

### 七、数据库与 API 可实现性

**通过项：**
- 05 为所有核心业务表定义了外键或逻辑关联
- 订单单据链追溯通过 `order_document_link` 表和子表直接 `order_id` 关联双重保障
- 幂等键、外部流水号、回调事件 ID（`integration_callback_event`）均在 05 和 06 中定义
- 操作日志（`audit_operation_log`）在 04/05/06 中完整定义
- API 请求/响应字段与领域模型、数据库字段基本一致（金额使用 `_cent`、状态使用英文编码）

**问题项：** 见 P1-02（健康检查接口缺失）、P1-03（Mock 场景控制接口缺失）、P1-07（回调验签失败响应规范缺失）、P2-10（统一文件下载接口缺失）

### 八、外部系统联调一致性

**通过项：**
- 06 的外部回调接口与 07 的外部系统联调链路完全一致
- 微信登录、手机号授权、微信支付、退款、物流、电子发票、企微审批、企微通知卡片、供货商 H5 均在 07 中有真实接入和 Mock 口径
- 各外部系统均保留了失败、异常待处理和人工补偿入口
- 未发现"后台直接开播/结束企微直播"、"强制获取手机号"、"自动通知必达"、"自动开票/红冲无人工兜底"、"外部失败全事务回滚"等高风险表述
- 系统设计约束.md 明确要求不承诺跨系统原子回滚，07 和 10/12 遵循了该约束
- Mock 与真实共用统一契约、状态枚举、幂等键和回调链路，不新增演示专用状态

**问题项：** 见 P1-07（回调验签失败响应格式缺失）

### 九、权限与安全

**通过项：**
- 08 权限矩阵覆盖了菜单、按钮、数据范围、字段脱敏、导出、审批、操作日志
- 讲师权限明确为"仅负责课程相关学员"（08 第 33/98/189 行）
- 代账人员权限明确为"仅访问授权财税与代账模块"（08 第 36/102/188 行）
- 敏感字段（手机号、地址、税号、邮箱、金额）在 08 第 7 节有详细的脱敏矩阵
- 高风险操作（退款、发货、开票、红冲、配置修改、导出）在 08 第 10 节有审计日志要求
- 06 接口文档中几乎所有写操作都标注了权限要求

**问题项：** 无阻塞问题。权限设计全面且可落地。

### 十、测试与验收覆盖

**通过项：**
- 09 覆盖了 01 的 15 条 P0 需求（P0 验收通过清单逐条映射）
- 09 覆盖了 02 的关键状态流转（第 7 节状态流转验收清单）
- 09 覆盖了 11 的关键业务规则（通过模块级用例和端到端用例间接覆盖）
- 09 覆盖了 12 的异常、幂等和人工补偿（4.11 节专门的外部协同、通知审批与异常补偿用例 + 4.12 节系统配置与审计）
- 09 覆盖了外部系统 Mock 和失败场景（第 6 节 Mock 场景矩阵）
- 09 覆盖了权限和数据范围（每个模块的 P 类用例 + 4.1/4.12 节专门的权限用例）

**问题项：** 见 P1-05（P0-15 Mock 闭环用例编号不完整）、P2-06（学习权益状态流转缺少独立验收项）

### 十一、部署与演示可执行性

**通过项：**
- 10 详细说明了 Mock 开关（环境变量 `INTEGRATION_*_MODE`）、演示账号（7.1 节初始化范围）、初始化数据（7.2 节演示数据清单）、启动顺序（5.1 节部署步骤总览）
- 10 第 8.5 节专门说明外部系统未接入时的替代演示口径
- 10 第 13 节演示流程覆盖了主业务闭环（线索→下单→支付→履约→退款→开票红冲→对账→代账材料→权限切换→异常补偿）
- 10 第 1 节明确"不默认继承历史失败产物中的旧代码、旧数据库、旧接口、旧架构、旧配置和旧部署脚本"

**问题项：** 见 P1-02（健康检查接口缺失影响探活）、P1-08（10 的修订建议未落实到前序文档）

---

## 原始文档质量评价（修复前记录）

| 维度 | 评分 | 简评 |
|---|---|---|
| 结构完整度 | ★★★★☆ | 00-15 齐全，但 00 清单遗漏 13/15 |
| 边界清晰度 | ★★★★★ | 每篇文档明确声明不继承旧系统 |
| 术语一致性 | ★★★★☆ | 核心术语一致，原型端"物流状态"需映射 |
| 状态机严谨性 | ★★★★★ | 四类状态拆分贯彻始终 |
| 链路闭环 | ★★★★★ | 线索到代账全链路可追溯 |
| 可实现性 | ★★★★☆ | 缺少健康检查和 Mock 场景控制接口 |
| 可测试性 | ★★★★★ | 09 覆盖全面，用例分级明确 |
| 可部署性 | ★★★★☆ | 10 内容详实但少许修订建议未落实 |
| 权限安全性 | ★★★★★ | 08 覆盖菜单/按钮/数据/脱敏/审计 |

---

*报告生成时间：2026-05-14*
*校验范围：22 份 Markdown 文档，11 个校验维度*
