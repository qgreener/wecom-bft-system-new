# S2-数据库与领域基础交接

## 1. 阶段范围

| 项目 | 内容 |
|---|---|
| 阶段编号 | S2 |
| 阶段名称 | 数据库与领域基础 |
| 工程根目录 | `D:\java-code\wecom-BFT-system` |
| 阶段定位 | 在 S0/S1 新系统基线下，落地核心领域枚举、基础值对象、核心业务表迁移、幂等唯一键、审计字段、订单单据链索引和安全 seed 框架。 |
| 已完成范围 | 核心枚举定义、金额分值对象、幂等键值对象、订单四状态快照、核心表 Flyway 迁移、迁移镜像、可选演示 seed、迁移和枚举测试。 |
| 未做范围 | 未实现完整订单创建、支付、退款、发货、开票、对账业务流程；未实现正式页面；未接真实外部系统；未写真实密钥；未读取或复用旧失败工程。 |
| 旧系统边界 | `D:\java-code\wecom-BFT-system-old-failed` 只作为隔离归档，不作为 S2 代码、数据库、接口、架构、配置或页面来源。 |

## 2. 输入依据

| 文档 | 路径 | S2 使用口径 |
|---|---|---|
| S0 交接 | `docs/新系统开发设计文档/S0-文档基线与工程确认交接.md` | 固定新系统目录、旧系统隔离、状态拆分和阶段交接格式。 |
| S1 交接 | `docs/新系统开发设计文档/S1-工程骨架与基础设施交接.md` | 固定后端骨架、Flyway 正式目录、迁移镜像目录、验证命令和 S2 边界。 |
| 业务流程与状态机 | `docs/新系统开发设计文档/02-业务流程与状态机说明.md` | 固定订单四类状态、子单据状态机、单据链和幂等补偿口径。 |
| 模块设计 | `docs/新系统开发设计文档/03-模块设计文档.md` | 固定模块边界、跨模块事件和不绕过订单单据链约束。 |
| 领域模型与数据字典 | `docs/新系统开发设计文档/04-领域模型与数据字典.md` | 固定领域对象、枚举编码、核心字段、唯一键和快照字段。 |
| 数据库设计 | `docs/新系统开发设计文档/05-数据库设计文档.md` | 固定核心表、字段类型、金额 `_cent`、审计字段、逻辑关联、索引和软删除策略。 |
| 关键业务规则 | `docs/新系统开发设计文档/11-关键业务规则与算法设计.md` | 固定金额计算、权益、库存、采购、开票、对账和通知规则的基础字段口径。 |
| 异常处理与幂等补偿 | `docs/新系统开发设计文档/12-异常处理与幂等补偿设计.md` | 固定幂等键分层、回调先落库、乱序回调、人工补偿和操作日志要求。 |
| 工程结构与开发规范 | `docs/新系统开发设计文档/14-工程结构与开发规范.md` | 固定包结构、迁移命名、数据库字段命名、枚举命名、测试命名和敏感配置禁令。 |

## 3. 修改文件与目录

| 类别 | 新增或修改 |
|---|---|
| 领域枚举 | `backend/src/main/java/com/wecombft/domain/enums/*.java` |
| 领域基础对象 | `backend/src/main/java/com/wecombft/domain/model/MoneyCent.java`、`backend/src/main/java/com/wecombft/domain/model/IdempotencyKey.java`、`backend/src/main/java/com/wecombft/domain/model/trade/OrderStatusSnapshot.java` |
| 正式迁移 | `backend/src/main/resources/db/migration/V202605141930__create_core_domain_schema.sql` |
| 迁移镜像 | `database/migration/V202605141930__create_core_domain_schema.sql` |
| Seed 框架 | `database/seed/README.md`、`database/seed/S2_minimal_safe_demo_seed.sql` |
| 测试 | `backend/src/test/java/com/wecombft/domain/enums/CoreEnumContractTest.java`、`backend/src/test/java/com/wecombft/domain/model/*.java`、`backend/src/test/java/com/wecombft/domain/model/trade/OrderStatusSnapshotTest.java`、`backend/src/test/java/com/wecombft/infrastructure/persistence/FlywayMigrationTest.java` |

## 4. 数据库迁移

| 项目 | 内容 |
|---|---|
| 正式 Flyway 目录 | `backend/src/main/resources/db/migration/` |
| 镜像目录 | `database/migration/` |
| S2 迁移脚本 | `V202605141930__create_core_domain_schema.sql` |
| 迁移验证 | `FlywayMigrationTest` 使用 H2 MySQL mode 验证干净库迁移、核心表、索引、金额字段和不可软删口径。 |
| 关键约束 | 未设计单一订单状态字段；订单状态拆为 `payment_status`、`fulfillment_status`、`refund_status`、`invoice_status`；金额字段统一 `_cent BIGINT`；状态字段保存英文编码。 |

## 5. 核心表清单

| 模块 | 表 |
|---|---|
| 用户与权限 | `sys_user`、`sys_role`、`sys_user_role` |
| 线索与学员 | `crm_lead`、`edu_student`、`student_address`、`student_invoice_title` |
| 课程与学习 | `course`、`course_spec`、`course_lesson_node`、`learning_entitlement` |
| 交易订单 | `trade_order`、`trade_order_item`、`order_document_link` |
| 支付退款 | `pay_payment`、`pay_refund`、`pay_refund_item` |
| 履约库存 | `fulfillment_shipment`、`fulfillment_shipment_item`、`logistics_trace`、`inventory_sku`、`inventory_stock_flow` |
| 采购供应 | `supplier`、`supplier_sku`、`purchase_order`、`purchase_order_item`、`purchase_receipt` |
| 发票税务 | `tax_rule`、`tax_invoice`、`tax_invoice_item` |
| 对账代账 | `finance_reconciliation_batch`、`finance_reconciliation_record`、`acct_material`、`acct_material_relation` |
| 通知审批、文件、回调、审计 | `notify_message`、`approval_record`、`sys_file_asset`、`integration_callback_event`、`audit_operation_log` |

交易单据、支付退款、发货物流、库存流水、采购、发票、对账、代账材料、通知审批、文件、回调事件和审计日志默认不加软删除字段。主数据如用户、角色、线索、学员、课程、规格、SKU、供货商、税务规则保留停用或软删除基础字段。

## 6. 幂等键、审计字段和单据链基础

| 能力 | 落点 |
|---|---|
| 订单创建唯一键 | `trade_order.order_no`、`trade_order.merchant_order_no` |
| 支付回调幂等 | `pay_payment.external_payment_no`、`pay_payment.idempotency_key` |
| 退款回调幂等 | `pay_refund.external_refund_no`、`pay_refund.idempotency_key` |
| 权益开通幂等 | `learning_entitlement.student_id + order_id + course_id` |
| 发货与物流幂等 | `fulfillment_shipment.shipment_no`、`fulfillment_shipment.tracking_no`、`logistics_trace.tracking_no + logistics_node_time + node_status` |
| 库存流水幂等 | `inventory_stock_flow.flow_no`、`inventory_stock_flow.idempotency_key` |
| 采购入库幂等 | `purchase_receipt.purchase_no + inbound_batch_no` |
| 开票与红冲幂等 | `tax_invoice.invoice_apply_no`、`invoice_no`、`red_invoice_no` |
| 对账导入幂等 | `finance_reconciliation_batch.bill_month + bill_source + file_digest` |
| 通知幂等 | `notify_message.idempotency_key + channel + receiver_user_id` |
| 回调先落库 | `integration_callback_event.source_system + event_no`、`idempotency_key` |
| 操作审计 | `audit_operation_log`，含 `trace_id`、操作人、目标对象、订单关联、前后快照、结果和失败原因 |
| 订单单据链 | `order_document_link.order_id + document_type + document_id`，并建立订单维度和单据维度索引 |

## 7. 核心枚举字典

| 枚举 | 编码 |
|---|---|
| `PaymentStatus` | `PENDING`、`PAID`、`CLOSED` |
| `PaymentResult` | `SUCCESS`、`FAILED`、`PROCESSING`、`CLOSED` |
| `FulfillmentStatus` | `NO_SHIPMENT`、`PENDING_SHIPMENT`、`SHIPPED`、`SIGNED` |
| `OrderRefundStatus` | `NONE`、`REVIEWING`、`REJECTED`、`PROCESSING`、`MANUAL_REQUIRED`、`FAILED`、`REFUNDED` |
| `OrderInvoiceStatus` | `NOT_APPLIED`、`APPLIED`、`TO_BE_ISSUED`、`ISSUED`、`RED_REVERSED` |
| `LeadStatus` | `PENDING_FOLLOW`、`CONTACTED`、`CONVERTED`、`ABANDONED` |
| `CourseStatus` | `DRAFT`、`PENDING_REVIEW`、`ON_SHELF`、`OFF_SHELF`、`DELETE_PENDING`、`DELETED` |
| `SpecStatus` | `ENABLED`、`DISABLED` |
| `LessonNodeType` | `CHAPTER`、`LESSON` |
| `LessonStatus` | `DRAFT`、`PUBLISHED`、`HIDDEN` |
| `EntitlementStatus` | `ACTIVE`、`FROZEN`、`REVOKED` |
| `ShipmentStatus` | `PENDING_SHIPMENT`、`SHIPPED`、`SIGNED` |
| `PurchaseStatus` | `APPROVING`、`APPROVAL_REJECTED`、`WAIT_CONFIRM`、`CONFIRMED`、`SHIPPED`、`REJECTED`、`COMPLETED`、`CANCELED` |
| `InputInvoiceStatus` | `NOT_INVOICED`、`INVOICED` |
| `ReconciliationResult` | `MATCHED`、`AMOUNT_DIFF`、`FEE_DIFF`、`UNMATCHED`、`DUPLICATE` |
| `AccountingMaterialStatus` | `PENDING_SUPPLEMENT`、`UPLOADED`、`CONFIRMED`、`CLOSED` |
| `NotificationChannel` | `IN_APP`、`WECHAT_SUBSCRIBE`、`WECOM_CARD` |
| `SendStatus` | `PENDING`、`SENT`、`FAILED`、`CANCELED` |
| `ReadStatus` | `UNREAD`、`READ` |
| `ApprovalStatus` | `PENDING`、`APPROVED`、`REJECTED`、`CANCELED` |
| `OperationResult` | `SUCCESS`、`FAILED` |

## 8. 领域基础对象

| 对象 | 路径 | 说明 |
|---|---|---|
| `MoneyCent` | `backend/src/main/java/com/wecombft/domain/model/MoneyCent.java` | 金额值对象，只承载非负分值计算，避免浮点金额。 |
| `IdempotencyKey` | `backend/src/main/java/com/wecombft/domain/model/IdempotencyKey.java` | 幂等键值对象，限制非空和最大 128 字符，支持业务片段组合。 |
| `OrderStatusSnapshot` | `backend/src/main/java/com/wecombft/domain/model/trade/OrderStatusSnapshot.java` | 订单四类摘要状态快照，仅表达初始虚拟/含实物订单状态，不提供单一订单状态。 |

## 9. Seed 数据说明

| 文件 | 是否自动执行 | 内容 |
|---|---|---|
| `database/seed/S2_minimal_safe_demo_seed.sql` | 否 | 本地或演示库可选执行，包含占位税务规则、供货商、SKU、课程和规格。 |
| `database/seed/README.md` | 否 | 说明 seed 不进入生产 Flyway 路径，只能在迁移完成后按需用于本地或演示库。 |

S2 seed 不包含真实用户、订单、支付、退款、发票、商户号、真实密钥、私钥、回调地址、SMTP 密码或生产数据。

## 10. 验证命令与结果

| 验证项 | 命令 | 结果 |
|---|---|---|
| Maven 测试与 Flyway 迁移 | `JAVA_HOME=D:\java\jdk-25.0.3; mvn -f backend/pom.xml test` | 通过，18 tests，0 failures，0 errors。 |
| 正式迁移与镜像一致性 | `Compare-Object` 对比正式迁移和镜像迁移 | 无差异。 |
| 单一状态字段扫描 | `rg --hidden -n "order_status" ...` | 排除 `docs/`、`target/`、`dist/`、`node_modules/` 后无命中。 |
| 旧失败工程引用扫描 | `rg --hidden -n "wecom-BFT-system-old-failed|old-failed" ...` | 排除 `docs/`、`target/`、`dist/`、`node_modules/` 后无命中。 |
| 裸金额列名扫描 | 扫描迁移、seed、后端源码和测试中的裸金额字段 | 无命中；金额列按 `_cent` 口径落地。 |

验证时当前 shell 未直接识别 `mvn`，且继承了 Java 17；实际使用本机 Maven `D:\java\apache-maven-3.9.15\bin\mvn.cmd` 并显式设置 `JAVA_HOME=D:\java\jdk-25.0.3`。

## 11. 接口变化

| 项目 | 内容 |
|---|---|
| 新增接口 | 无。S2 不实现正式业务接口。 |
| 现有接口 | `/api/health`、`/api/health/ready` 保持 S1 口径。 |

## 12. 页面证据

| 项目 | 内容 |
|---|---|
| 正式页面 | 无。S2 不实现正式页面。 |
| 截图/录屏 | 无。S2 验收以迁移、枚举和值对象测试为准。 |

## 13. 冲突记录与修订建议

| 编号 | 类型 | 记录 | 处理结果 | 阻断 S3 |
|---|---|---|---|---|
| S2-C1 | 环境依赖 | 当前 shell 继承 Java 17，项目要求 Java 25。 | 验证时显式设置 `JAVA_HOME=D:\java\jdk-25.0.3`。建议后续统一本地启动脚本加载机器级 `JAVA_HOME`。 | 否 |
| S2-C2 | 工程状态 | 当前目录不是 Git 仓库。 | 已直接在新系统目录落地文件；未创建提交。 | 否 |
| S2-C3 | 真实库验证 | 本机未启动真实 MySQL。 | 使用 H2 MySQL mode 验证 Flyway；后续具备 MySQL 环境时补跑真实库迁移。 | 否 |

| 修订建议 | 涉及文档 | 优先级 | 说明 |
|---|---|---|---|
| 在文档索引中补充 S2 交接文档 | `00-文档说明.md` 或阶段索引 | P2 | 方便后续阶段从文档清单发现交接入口。 |
| Maven/JDK 环境加载口径可写入本地启动说明 | `10-服务器部署与演示说明.md` 或 `14-工程结构与开发规范.md` | P2 | 当前验证需要显式切换 Java 25，后续脚本可统一处理。 |

## 14. 遗留依赖问题

| 问题 | 当前状态 | 后续建议 |
|---|---|---|
| Maven PATH | 当前 shell 未直接识别 `mvn`。 | 后续命令可继续使用 `D:\java\apache-maven-3.9.15\bin\mvn.cmd` 或修正用户 PATH。 |
| Java 版本 | 当前 shell 默认 `JAVA_HOME=C:\Java17`，项目编译要求 Java 25。 | 后续阶段验证前显式设置 `JAVA_HOME=D:\java\jdk-25.0.3`。 |
| Docker 不可用 | 沿用 S1 遗留，本机受 WSL 限制未运行 Compose。 | S12 或具备 Docker 的环境补验证。 |
| MySQL 未启动 | S2 未连接真实 MySQL。 | S3/S12 可在真实 MySQL 环境补跑迁移。 |
| Git 仓库缺失 | 当前目录不是 Git 仓库。 | 如需要版本管理，由项目负责人初始化 Git。 |

## 15. S3 输入边界

| 项目 | S3 规则 |
|---|---|
| 阶段名称 | 用户、角色、权限、系统配置、操作日志 |
| 进入条件 | S2 已完成用户、角色、审计、回调、文件和核心业务表迁移；核心枚举、幂等键和值对象已可被后续代码复用。 |
| 必读文档 | `01-需求规格说明书.md`、`06-API接口文档.md`、`08-权限矩阵文档.md`、`10-服务器部署与演示说明.md`、`11-关键业务规则与算法设计.md`、`12-异常处理与幂等补偿设计.md`、`14-工程结构与开发规范.md`、S0/S1/S2 交接。 |
| 主要产出 | 登录或测试登录基础、角色申请、权限返回、菜单按钮、数据范围、字段脱敏、系统配置接口、操作日志落库和基础审批待办。 |
| S3 可做 | 实现权限基础接口、配置接口、审计日志写入能力、角色申请和最小权限 seed；补权限、脱敏、审计测试。 |
| S3 不做 | 不实现具体订单、支付、退款、发货、开票、对账业务处理；不实现完整正式页面；不接真实外部系统；不写真实密钥；不复用旧失败工程。 |
| 强约束 | 前端菜单隐藏不能替代后端鉴权；导出、配置、权限、退款、发货、采购、开票、对账等敏感动作必须可审计；字段脱敏以后端为准；真实密钥不得入库、入文档、入前端或入日志。 |
| 验收口径 | 未分配角色只能申请角色；不同角色菜单、按钮、接口、数据范围和脱敏结果可验证；配置只保存安全占位或脱敏值；操作日志可按 `trace_id`、目标对象、订单关联追溯；Maven 测试通过。 |

## 16. S3 提示词

```text
请负责 S3“用户、角色、权限、系统配置、操作日志”。

背景澄清：
当前目录 D:\java-code\wecom-BFT-system 是干净的新系统工作目录；旧失败工程位于 D:\java-code\wecom-BFT-system-old-failed。不得读取、复制或复用旧失败工程中的代码、数据库、接口、架构、配置和页面实现。

先读取：
1. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\S0-文档基线与工程确认交接.md
2. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\S1-工程骨架与基础设施交接.md
3. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\S2-数据库与领域基础交接.md
4. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\01-需求规格说明书.md
5. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\06-API接口文档.md
6. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\08-权限矩阵文档.md
7. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\10-服务器部署与演示说明.md
8. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\11-关键业务规则与算法设计.md
9. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\12-异常处理与幂等补偿设计.md
10. D:\java-code\wecom-BFT-system\docs\新系统开发设计文档\14-工程结构与开发规范.md

S2 已完成：
- 后端骨架位于 backend/
- Flyway 正式迁移目录为 backend/src/main/resources/db/migration/
- 迁移镜像目录为 database/migration/
- 核心领域表已由 V202605141930__create_core_domain_schema.sql 落地
- 已有 sys_user、sys_role、sys_user_role、audit_operation_log、approval_record、notify_message、sys_file_asset、integration_callback_event 等 S3 可用基础表
- 已定义核心枚举和值对象：MoneyCent、IdempotencyKey、OrderStatusSnapshot
- 订单状态已拆分为 payment_status、fulfillment_status、refund_status、invoice_status，禁止单一 order_status
- Maven 测试已通过；当前 shell 如无法识别 mvn，使用 D:\java\apache-maven-3.9.15\bin\mvn.cmd，并显式设置 JAVA_HOME=D:\java\jdk-25.0.3

目标：
按照 S0/S1/S2 基线，在当前新系统工程中落地 S3“用户、角色、权限、系统配置、操作日志”。

本阶段只做：
- 用户、角色、用户角色授权基础
- 测试登录或最小登录基础
- 角色申请或最小审批待办基础
- 权限返回接口，包括菜单、按钮、数据范围、字段脱敏规则
- 系统配置基础接口和安全配置占位/脱敏展示
- 操作日志写入基础
- 最小安全权限 seed
- 权限、脱敏、审计、配置基础测试

本阶段不做：
- 不实现完整订单创建、支付、退款、发货、开票、对账业务流程
- 不实现正式业务页面
- 不接真实外部系统
- 不写真实密钥、商户号、私钥、真实回调地址或 SMTP 密码
- 不读取或复用 D:\java-code\wecom-BFT-system-old-failed

强约束：
- 前端菜单隐藏不能替代后端鉴权
- 未分配角色用户只能进入角色申请或最小允许入口
- 后端必须校验接口权限、按钮权限、数据范围和字段脱敏
- 配置接口不得返回明文密钥、私钥、完整 token、SMTP 密码或真实回调敏感信息
- 操作日志不可编辑、不可删除；敏感操作必须写 audit_operation_log
- Mock、沙箱、真实接入不得分叉权限、配置、审计模型
- 不得引入单一 order_status 或任何旧状态口径

验收要求：
- Maven 测试通过
- 未分配角色只能申请角色
- 至少管理员、运营/客服、仓管、代账等基础角色的菜单/按钮/数据范围/脱敏口径可验证
- 权限返回接口不泄露敏感配置
- 配置保存和展示使用占位或脱敏值
- 操作日志可按 trace_id、target_type、target_id、order_id 追溯
- 不出现真实密钥、商户号、私钥、真实回调地址、SMTP 密码
- 不读取或复用旧失败工程

完成后请交接：
- 新增/修改的迁移脚本或 seed
- 用户角色权限模型说明
- 权限接口清单
- 系统配置与脱敏说明
- 操作日志能力说明
- 验证命令与结果
- 遗留依赖问题
- S4 输入边界
```
