# S3-用户、角色、权限、系统配置、操作日志交接

## 1. 阶段范围

| 项目 | 内容 |
|---|---|
| 阶段编号 | S3 |
| 阶段名称 | 用户、角色、权限、系统配置、操作日志 |
| 工程根目录 | `D:\java-code\wecom-BFT-system` |
| 已完成范围 | 最小测试登录、当前用户权限返回、未分配角色申请、管理员审批并授权、系统配置查询与保存、配置敏感值脱敏、操作日志写入与筛选、最小安全 seed、权限/配置/审计测试。 |
| 未做范围 | 未实现完整订单、支付、退款、发货、开票、对账业务流程；未实现正式业务页面；未接真实外部系统；未写真实密钥、商户号、私钥、真实回调地址或 SMTP 密码。 |
| 旧系统边界 | 本阶段只在当前新系统工程内读取和实现；未读取、复制或复用旧失败工程代码、数据库、接口、架构、配置和页面实现。 |

## 2. 输入依据

| 文档 | 使用口径 |
|---|---|
| `S0-文档基线与工程确认交接.md` | 固定新系统目录、旧系统隔离、阶段交接格式。 |
| `S1-工程骨架与基础设施交接.md` | 固定后端骨架、Flyway 正式目录、迁移镜像目录、验证命令。 |
| `S2-数据库与领域基础交接.md` | 固定已有用户、角色、审计、审批基础表和 S3 输入边界。 |
| `01-需求规格说明书.md` | 固定未分配角色申请、角色权限、操作日志和配置审计验收口径。 |
| `06-API接口文档.md` | 固定 `/api/admin/auth/me`、角色申请、系统配置、操作日志接口契约。 |
| `08-权限矩阵文档.md` | 固定菜单、按钮、数据范围、字段脱敏、审计和导出控制原则。 |
| `10-服务器部署与演示说明.md` | 固定配置占位、Mock 模式和部署敏感信息禁令。 |
| `11-关键业务规则与算法设计.md` | 固定权限判定顺序、数据范围、字段脱敏和操作审计规则。 |
| `12-异常处理与幂等补偿设计.md` | 固定审计不可变、敏感日志脱敏和异常追踪口径。 |
| `14-工程结构与开发规范.md` | 固定后端分层、迁移命名、接口前缀、测试命名和敏感配置规范。 |

## 3. 新增/修改文件

| 类别 | 文件 |
|---|---|
| Flyway 正式迁移 | `backend/src/main/resources/db/migration/V202605142010__s3_security_config_seed.sql` |
| 迁移镜像 | `database/migration/V202605142010__s3_security_config_seed.sql` |
| 权限与登录 | `backend/src/main/java/com/wecombft/infrastructure/security/*`、`backend/src/main/java/com/wecombft/application/iam/*`、`backend/src/main/java/com/wecombft/interfaces/admin/AdminAuthController.java` |
| 角色申请与授权 | `backend/src/main/java/com/wecombft/application/iam/RoleApplicationService.java`、`backend/src/main/java/com/wecombft/interfaces/admin/RoleApplicationController.java`、`backend/src/main/java/com/wecombft/infrastructure/persistence/iam/*` |
| 系统配置 | `backend/src/main/java/com/wecombft/application/system/SystemConfigService.java`、`backend/src/main/java/com/wecombft/interfaces/admin/SystemConfigController.java`、`backend/src/main/java/com/wecombft/infrastructure/persistence/system/*` |
| 操作日志 | `backend/src/main/java/com/wecombft/application/audit/AuditLogService.java`、`backend/src/main/java/com/wecombft/interfaces/admin/AuditLogController.java`、`backend/src/main/java/com/wecombft/infrastructure/persistence/audit/*` |
| 通用能力 | `backend/src/main/java/com/wecombft/shared/web/ApiResponse.java`、`ApiException.java`、`GlobalExceptionHandler.java`、`backend/src/main/java/com/wecombft/shared/id/IdGenerator.java` |
| 测试 | `backend/src/test/java/com/wecombft/interfaces/admin/*`、`backend/src/test/java/com/wecombft/infrastructure/persistence/FlywayMigrationTest.java` |

## 4. 迁移脚本与 seed

| 项目 | 内容 |
|---|---|
| 新表 | `sys_config`，包含 `config_group`、`config_key`、`config_value`、`masked_value`、`sensitive_flag`、`editable_flag`、审计字段和版本字段。 |
| 索引 | `uk_sys_config_group_key(config_group, config_key)`、`idx_sys_config_group(config_group)`、`idx_sys_config_sensitive(sensitive_flag)`。 |
| 最小角色 | `SUPER_ADMIN`、`OPS`、`SERVICE`、`WAREHOUSE`、`ACCOUNTING`。 |
| 最小用户 | `DEMO_ADMIN`、`DEMO_OPS`、`DEMO_SERVICE`、`DEMO_WAREHOUSE`、`DEMO_ACCOUNTING`、`DEMO_UNASSIGNED`。 |
| 初始授权 | 除 `DEMO_UNASSIGNED` 外，其余演示用户均有一个有效角色。 |
| 初始配置 | `PAYMENT`、`WECOM`、`MESSAGE`、`LOGISTICS`、`INVOICE` 分组；敏感项仅保存 `<SENSITIVE_SET>` 和 `********`。 |

## 5. 用户角色权限模型

| 层级 | 落点 | 说明 |
|---|---|---|
| 用户 | `sys_user` | 当前 S3 只实现内部管理端测试登录，令牌格式为 `S3-DEMO-<user_no>`，不作为正式认证方案。 |
| 角色 | `sys_role` | 角色由 seed 初始化，`role_code` 是权限目录主键，`data_scope` 作为数据库侧基础范围口径。 |
| 用户角色 | `sys_user_role` | 管理员审批角色申请后写入或激活授权记录，`grant_status=ACTIVE` 生效。 |
| 权限目录 | `PermissionCatalog` | 后端静态目录合并菜单、按钮权限、数据范围和字段脱敏规则；前端只消费结果，后端接口仍二次校验。 |
| 未分配角色 | `PermissionCatalog` 的 `UNASSIGNED` 策略 | 仅返回 `iam:role-application:create`、`role.application` 菜单和 `NONE` 数据范围。 |

## 6. 权限接口清单

| 接口 | 方法 | 权限 | 说明 |
|---|---|---|---|
| `/api/admin/auth/test-login` | `POST` | 无，测试入口 | 通过 `user_no` 返回 S3 测试令牌。 |
| `/api/admin/auth/me` | `GET` | 已登录内部用户 | 返回 `roles[]`、`permission_codes[]`、`data_scope`、`menus[]`、`field_masks[]`。 |
| `/api/admin/role-applications` | `POST` | 未分配角色用户 | 提交角色申请，生成 `approval_record`，写 `audit_operation_log`。 |
| `/api/collab/approvals/{approval_id}/actions` | `POST` | `iam:role-application:approve` | 管理员审批角色申请；通过后写 `sys_user_role` 并审计。 |
| `/api/admin/system/configs` | `GET` | 管理员或特定配置只读权限 | 查询配置分组，敏感项仅返回 `masked_value`。 |
| `/api/admin/system/configs` | `POST` | `system:config:write` | 保存配置，敏感项不保存明文，写审计日志。 |
| `/api/admin/audit/logs` | `GET` | `system:audit:read` | 支持按 `trace_id`、`target_type`、`target_id`、`order_id`、操作人和操作类型筛选。 |

## 7. 系统配置与脱敏

| 项目 | 口径 |
|---|---|
| 查询返回 | 只返回 `config_key`、`display_name`、`masked_value`、`editable_flag`、更新时间、更新人和版本，不返回 `plain_value`。 |
| 敏感保存 | `sensitive_flag=1` 的配置保存时统一落 `config_value=<SENSITIVE_SET>`、`masked_value=********`。 |
| 非敏感保存 | 如 `INTEGRATION_PAYMENT_MODE=mock` 可保存原始非敏感值。 |
| 禁止内容 | 未写入真实密钥、商户号、私钥、真实回调地址或 SMTP 密码。 |
| 审计快照 | 配置保存审计只记录配置分组和更新键，不记录敏感配置明文。 |

## 8. 操作日志能力

| 能力 | 落点 |
|---|---|
| 写入 | `AuditLogService.writeSuccess` 写入 `audit_operation_log`，已接入角色申请提交、角色审批、配置保存。 |
| 不可变更 | 未提供编辑或删除接口；审计表沿用 S2 不软删口径。 |
| 查询 | `/api/admin/audit/logs` 支持 `trace_id`、`target_type`、`target_id`、`order_id` 等筛选。 |
| 脱敏 | 审计写入服务会对手机号和密钥类摘要进行兜底脱敏，接口列表不返回前后快照正文。 |

## 9. 验证命令与结果

| 验证项 | 命令 | 结果 |
|---|---|---|
| Maven 测试 | `JAVA_HOME=D:\java\jdk-25.0.3; D:\java\apache-maven-3.9.15\bin\mvn.cmd -f backend\pom.xml test` | 通过，27 tests，0 failures，0 errors。 |
| 迁移镜像一致性 | `Compare-Object` 对比正式迁移和镜像迁移 | 无差异。 |
| 单一状态字段扫描 | 排除 `docs/`、`target/`、`dist/`、`node_modules/` 后扫描 `order_status` | 无命中。 |
| 旧失败工程复用扫描 | 排除 `docs/`、`target/`、`dist/`、`node_modules/`、`.AGENT.md` 后扫描旧失败工程路径 | 无命中。 |

## 10. 遗留依赖问题

| 问题 | 当前状态 | 后续建议 |
|---|---|---|
| 正式认证 | S3 使用测试登录令牌，不是企业微信扫码正式认证。 | S4 或认证阶段接入企微扫码/工作台登录，保留权限模型不分叉。 |
| 权限持久化粒度 | S3 菜单、按钮、数据范围和脱敏规则由后端静态目录承载。 | 后续如需后台配置权限矩阵，可在不改变接口契约的前提下新增权限表。 |
| 数据范围真实业务落点 | S3 只完成权限返回和接口权限校验，没有订单、线索等真实业务列表。 | S4 开始在业务查询中注入数据范围过滤。 |
| 配置密钥管理 | S3 仅占位/脱敏保存，不接密钥管理系统。 | 真实接入时从环境变量或密钥管理读取，不把明文写入库和日志。 |
| Docker/MySQL 真实环境 | 沿用 S1/S2 遗留，本机仍以 H2 MySQL mode 验证。 | 具备 MySQL 环境后补跑真实库 Flyway。 |

## 11. S4 输入边界

| 项目 | S4 建议 |
|---|---|
| 进入条件 | S3 权限、配置、审计、角色申请和最小 seed 已可用；Maven 测试通过。 |
| 可复用能力 | `AdminSessionService`、`PermissionCatalog`、`AuditLogService`、`SystemConfigService`、`sys_config`、S3 安全 seed。 |
| 下一阶段应做 | 线索/学员/课程等首个业务域接口接入登录态、按钮权限、数据范围、字段脱敏和审计写入。 |
| 继续禁止 | 不引入单一 `order_status`；不接真实外部系统；不写真实密钥、商户号、私钥、真实回调地址或 SMTP 密码；不复用旧失败工程。 |
