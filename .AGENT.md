# Agent 环境说明

本文档记录当前工作目录的本机环境事实和后续 agent 执行约束。所有后续开发以当前新系统工程 `D:\java-code\wecom-BFT-system` 为准；旧失败工程 `D:\java-code\wecom-BFT-system-old-failed` 不得读取、复制或复用。

## 1. 当前仓库状态

| 项目 | 说明 |
|---|---|
| 工作目录 | `D:\java-code\wecom-BFT-system` |
| Git 状态 | 已初始化 Git 仓库 |
| 当前分支 | `master` |
| 当前基线提交 | `dedf39c chore: establish s0-s2 baseline` |
| 忽略目录 | `backend/target/`、`frontend/**/dist/`、`node_modules/`、本地 `.env` 已被 `.gitignore` 排除 |
| 换行与路径 | 本仓库设置 `core.autocrlf=false`、`core.quotepath=false`，并新增 `.gitattributes` 管理换行 |

## 2. Java

| 项目 | 当前情况 |
|---|---|
| 项目要求 | `backend/pom.xml` 使用 Java 25：`java.version=25`、`maven.compiler.release=25` |
| 当前 shell 默认 | `JAVA_HOME=C:\Java17`，`java -version` / `javac -version` 默认是 Java 17.0.8 |
| 可用 Java 25 | `D:\java\jdk-25.0.3` |
| 机器级 JAVA_HOME | `D:\java\jdk-25.0.3` |
| 注意事项 | 直接运行 `mvn` 或直接使用当前 shell 默认 Java 可能出现“不支持发行版本 25” |

后端测试或打包前，建议在同一 PowerShell 命令中显式设置：

```powershell
$env:JAVA_HOME='D:\java\jdk-25.0.3'
$env:Path="$env:JAVA_HOME\bin;D:\java\apache-maven-3.9.15\bin;$env:Path"
```

## 3. Maven

| 项目 | 当前情况 |
|---|---|
| `mvn` PATH | 当前 shell 中 `where mvn` 未找到全局 `mvn` |
| 可用 Maven | `D:\java\apache-maven-3.9.15\bin\mvn.cmd` |
| Maven 版本 | Apache Maven 3.9.15 |
| Java 25 下验证 | 使用 `JAVA_HOME=D:\java\jdk-25.0.3` 时，Maven 识别 Java 25.0.3 |

推荐后端验证命令：

```powershell
$env:JAVA_HOME='D:\java\jdk-25.0.3'
$env:Path="$env:JAVA_HOME\bin;D:\java\apache-maven-3.9.15\bin;$env:Path"
& 'D:\java\apache-maven-3.9.15\bin\mvn.cmd' -f backend/pom.xml test
```

S2 基线下该命令已验证通过：18 tests，0 failures，0 errors。

## 4. Docker

| 项目 | 当前情况 |
|---|---|
| Docker CLI | 当前 shell 中 `docker` 命令不可用 |
| Docker Compose | 当前 shell 中 `docker compose` 不可用 |
| S1/S2 状态 | Docker Compose、Nginx、systemd 模板已存在，但本机未验证容器启动 |
| 后续约束 | 不要把 Docker 作为已可用前提；需要容器验证时，应先修复本机 Docker/WSL 环境或换到可用 Docker 的机器 |

相关文件：

| 文件 | 用途 |
|---|---|
| `deploy/docker-compose.yml` | MySQL、Redis、MinIO、后端等容器模板 |
| `deploy/env/.env.example` | Docker Compose 环境变量占位模板 |
| `deploy/nginx/wecom-bft.conf.template` | Nginx 模板 |

## 5. MySQL

| 项目 | 当前情况 |
|---|---|
| MySQL 客户端 | `D:\mysql\bin\mysql.exe` |
| 客户端版本 | MySQL Community Server 9.1.0 for Win64 |
| Windows 服务 | `MySQL80` 正在运行，自动启动；`MySQL91` 已停止，手动启动 |
| 端口探测 | `127.0.0.1:3306` 当前开放 |
| 凭据与库 | 未验证具体账号、密码、数据库名和 Flyway 真实 MySQL 迁移 |
| S2 验证方式 | 后端测试 profile 使用 H2 MySQL mode 验证 Flyway 迁移 |

后续如需真实 MySQL 验证，应先确认：

| 检查项 | 建议 |
|---|---|
| 数据库名 | 建议使用独立本地库，例如 `wecom_bft_dev` |
| 凭据来源 | 只从 `.env.local`、本机环境变量或私有配置读取，不提交仓库 |
| 迁移验证 | 使用 Flyway 对空库执行正式迁移，不能手工改库后不补迁移 |
| 敏感信息 | 不把真实密码、商户号、私钥、回调地址写入仓库或前端 |

## 6. Redis

| 项目 | 当前情况 |
|---|---|
| Redis CLI | 当前 shell 中未找到 `redis-cli` |
| Windows 服务 | 未发现 Redis 相关服务 |
| 端口探测 | `127.0.0.1:6379` 当前关闭 |
| 后续约束 | 不要把 Redis 当作已可用依赖；S3 如涉及登录态、幂等缓存或会话，应先选择内存/数据库兜底或补齐 Redis 启动说明 |

## 7. MinIO

| 项目 | 当前情况 |
|---|---|
| MinIO 服务端 CLI | 当前 shell 中未找到 `minio` |
| MinIO 客户端 CLI | 当前 shell 中未找到 `mc` |
| 端口探测 | `127.0.0.1:9000`、`127.0.0.1:9001` 当前关闭 |
| 后续约束 | 不要把 MinIO 当作已可用依赖；文件上传下载阶段应先确认对象存储启动、Bucket、访问密钥来源和下载鉴权 |

## 8. Node 与 pnpm

| 项目 | 当前情况 |
|---|---|
| Node.js | `v24.14.1` |
| pnpm | `11.1.1` |
| 前端依赖 | `node_modules/` 和各前端子项目 `node_modules/` 已存在但被 Git 忽略 |
| 构建产物 | `frontend/**/dist/` 已存在但被 Git 忽略 |

常用前端命令：

```powershell
pnpm install
pnpm build
```

## 9. 后续 Agent 执行约束

| 约束 | 要求 |
|---|---|
| 回答语言 | 与当前项目约定一致，回答使用中文 |
| 旧工程边界 | 不读取、不复制、不复用 `D:\java-code\wecom-BFT-system-old-failed` |
| Java/Maven | 后端验证前显式使用 Java 25 和 Maven 3.9.15 |
| Docker | Docker 当前不可用，不得声称 Docker Compose 已验证 |
| MySQL | 仅确认 3306 开放和 MySQL80 运行；真实库迁移仍需单独验证 |
| Redis/MinIO | 当前未运行；涉及功能时先补启动或降级策略 |
| 密钥安全 | 真实密钥、商户号、私钥、回调地址、SMTP 密码不得进入仓库、前端或日志 |
| Git | S0-S2 已提交为基线；后续阶段应在提交前运行必要测试并检查 `git status` |
| Git 差异检查 | `git diff --stat` 不显示 untracked 文件；新增文件较多时必须使用 `git status --short --untracked-files=all` 确认完整文件清单 |
| 提交流程 | 用户已明确要求“直接提交”时，不再展开通用收尾流程；只做状态检查、提交和提交后确认 |
